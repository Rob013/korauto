// Temporary stub for AdminCarSearch to fix build errors
import React from 'react';

const AdminCarSearch: React.FC<{ className?: string }> = ({ className }) => {
  return (
    <div className={className}>
      <p>Admin Car Search temporarily disabled</p>
    </div>
  );
};

export default AdminCarSearch;